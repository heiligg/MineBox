# Final Release Audit — MineBox OS Prototype v1.0

**Date:** 2026-08-03  
**Version:** `1.0.0-prototype.1`  
**Scope:** Active tree `MineBox-OS-Builder-v0.2` (Checkpoint 8)

## Classification legend

| Class | Meaning |
|-------|---------|
| RELEASE BLOCKER | Must fix before packaging claim |
| DOCUMENTED LIMITATION | Acceptable for prototype if disclosed |
| DEVELOPMENT ONLY | Allowed in tree; excluded from production defaults |
| SAFE TO KEEP | Intentional |
| REMOVE FROM RELEASE | Strip from archives |

## Blockers found and disposition

| Finding | Class | Disposition |
|---------|-------|-------------|
| API `setdefault(MINEBOX_FORCE_MOCK_HARDWARE=1)` on every startup | RELEASE BLOCKER | **Fixed** — removed; mock only off-Pi / explicit env |
| Version drift (`1.4.0-os-preview` / API `0.2.0` / docs v1.0) | RELEASE BLOCKER | **Fixed** — centralized `VERSION` = `1.0.0-prototype.1` |
| OTA/update path hard-coded `wlan0` drop-in/sysctl | RELEASE BLOCKER | **Fixed** — prefers `minebox_render_hotspot_configs.py` |
| `HOTSPOT_INTERFACE = "wlan0"` constant in `network.py` | RELEASE BLOCKER | **Fixed** — uses `hotspot_interface()` |
| Captive portal advertised bootstrap PSK as “usual” | DOCUMENTED LIMITATION → UX | **Fixed** — generic post-rotation wording |
| Missing CP8 docs / release archive | RELEASE BLOCKER | **Addressed** in this checkpoint |

## Documented limitations (kept)

| Finding | Notes |
|---------|-------|
| Bootstrap hotspot PSK `mineboxsetup` in templates | First-boot **requires** rotation |
| Image OS user `minebox`/`minebox`, SSH enabled | Rotate before distribution |
| Encoder / LED / fan GPIO `NOT_CONFIGURED` | Awaiting PCB pinout |
| CM5 profile stub | Placeholder |
| Plymouth assets placeholder | Theme prepared, not physically validated |
| SoftAP templates seed `wlan0` at bake time | First boot re-renders from roles |
| No Minecraft `server.jar` in image | License |
| Physical Pi flash / dual-radio not validated here | Honest status in manifest |
| Git OTA prototype | Not production updater |
| Tailscale optional / not baked | Provider ready; binary optional |

## Development only (safe in tree)

- `MINEBOX_DEV_MODE` / OpenAPI docs routes
- `POST /api/v1/display/events/inject` (loopback + token)
- `minebox_display_dev.py`, tests with mock hardware
- `.test-runtime/` (gitignored)

## Remove from release archives

| Item | Action |
|------|--------|
| `__pycache__`, `*.pyc`, `.pytest_cache` | Excluded |
| `.test-runtime/`, `runtime/`, `.venv/`, `.build/`, `output/` | Excluded |
| `*.backup`, `*.bak`, worlds, backups, auth DBs | Excluded |
| Stale `pi-gen/.../files/minebox/` embed | Excluded from package; regenerated by `build.sh` |
| `.git`, IDE metadata | Excluded |
| Live secrets / Tailscale state / session files | Never packaged |

## Security production checks

| Check | Result |
|-------|--------|
| OpenAPI disabled by default | PASS |
| Dev routes gated | PASS |
| Mock hardware not forced on Pi | PASS (after fix) |
| No committed admin password | PASS (setup creates hash) |
| Hotspot bootstrap requires rotation | PASS |
| RCON randomized / legacy constant is migration marker only | PASS |
| SSH policy matches docs (`ENABLE_SSH=1`, rotate password) | PASS |
| API runs as `minebox` (unit User=) | PASS |
| Display token generated at runtime | PASS |
| No private keys in tree | PASS |

## Duplicate / obsolete paths

| Item | Class |
|------|--------|
| `app/install.sh` | SAFE TO KEEP (canonical) |
| Identical systemd copies in app / pi-gen / systemd/ | SAFE TO KEEP |
| Hotspot template duplicates | SAFE TO KEEP (seeds) |
| Stale nested `files/minebox/install.sh` | REMOVE FROM RELEASE |
| `minebox_updater.py` (dev-mode) | DEVELOPMENT ONLY — prefer `minebox_update_apply.py` |
| `docs/v1/Repository_Audit.md` (CP1 snapshot) | DOCUMENTED LIMITATION — historical; prefer this audit |

## Image / hardware validation status

| Area | Status |
|------|--------|
| Automated software tests | Validated in CP8 |
| pi-gen structure / packages / scripts | Statically validated |
| Full Docker image build | **NOT GENERATED IN THIS ENVIRONMENT** (Windows host) |
| Physical Raspberry Pi validation | **NOT PERFORMED** |

## Release gate recommendation

**B. RELEASED — SOFTWARE PACKAGE VALIDATED, IMAGE BUILD PENDING**

(or **A** if a subsequent Linux CI run produces a verified `.img.xz` without changing this audit’s hardware claims).
